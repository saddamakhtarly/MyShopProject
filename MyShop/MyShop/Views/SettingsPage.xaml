<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MyShop.Views.SettingsPage">
    <ContentPage.Content>
        <StackLayout Padding="20">
            <StackLayout x:Name="itemCreationStack" IsVisible="False" Padding="20">
                <Button Text="Create Category" Command="{Binding CreateCategory}"/>
                <Button Text="Create Product" Command="{Binding CreateProduct}"/>
                <Button Text="Order History" Command="{Binding OrderHistory}"/>
            </StackLayout>
            
            <Button Text="Logout" Command="{Binding Logout}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>